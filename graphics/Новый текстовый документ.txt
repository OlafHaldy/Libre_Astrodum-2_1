theme.py - сердце оформления. Любой цвет, любой размер, любой шрифт меняется только здесь.
geometry.py - Именно он будет отвечать за всю математику. После этого остальные файлы практически не будут содержать вычислений.
zodiac.py - модуль отвечает толкьо за кольцо знаков


_________________
Теперь нужно изменить wheel_renderer.py
Добавить импорт:
______________________
from graphics.zodiac import ZodiacLayer

В методе build() после кругов добавить:

self.draw_background()
self.draw_circles()

ZodiacLayer(self).draw()

self.draw_axes()

houses.py будет заниматься только домами.

Он не знает ничего о планетах.
Он не знает ничего о знаках.
Он просто рисует дома.
___________________________________
Теперь обновляем wheel_renderer.py
___________________________________
Добавляем импорт:

from graphics.houses import HousesLayer

И меняем build():

def build(self):

    self.draw_background()

    self.draw_circles()

    ZodiacLayer(self).draw()

    HousesLayer(self).draw()

    self.draw_axes()

planets.py - Это один из самых важных файлов. Именно он делает колесо "живым". 
Пока что я предлагаю сделать архитектурно правильную версию 1, без сложного алгоритма антиколлизии. 
После того как всё заработает, мы добавим интеллектуальное раздвижение планет (как в ZET).
__________________________________
Теперь обновляем wheel_renderer.py
__________________________________
Добавляем импорт:

from graphics.planets import PlanetsLayer

И метод build() становится:

def build(self):

    self.draw_background()

    self.draw_circles()

    ZodiacLayer(self).draw()

    HousesLayer(self).draw()

    PlanetsLayer(self).draw()

    self.draw_axes()

После того как эта версия заработает, сделать Liber Astrodum Smart Layout 2.0 — практически копию логики ZET. 
Это будет уже не "радиус +15", а полноценный движок размещения, 
который автоматически строит красивые кластеры планет и станет одной из сильных сторон нашего проекта. 
Именно к этому, как мне кажется, стоит стремиться.